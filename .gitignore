# Agent scratch / verification harness — not part of the app
tools/

# Backups
*.bak

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Local Claude Code settings
.claude/

# Supabase CLI scratch (project ref cache, version checks)
supabase/.temp/

# Brand source artwork — the app embeds a derived, cropped copy as a data URI
NIVA_Bupa_Logo*
